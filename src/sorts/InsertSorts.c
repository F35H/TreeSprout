
InsertSort(int[] *arrSize, int arrSize){
  int i = 1;
  int j;
    for(;i< arrSize; ++i ){
      if (*arr[i - 1] > *arr[i]){
        for (j = i; j != 0; j-- ){
          swap(arr, (j-1), j);} } } };
          
GnomeSort(int[] *arrSize, int arrSize){
  int i = 0;
  bool s;
  for(;i< arrSize; i++){
    if (*arr[i] < *arr[i - j]){
      swap(arr, (j-1), j);      
      i--; } } };
      
