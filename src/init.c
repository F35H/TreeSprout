#include "engDef.h"

gen::init(){
  short i = 3;
  bool *arrCnt = malloc(4*sizeof(bppl));
  bool **arrChck = malloc(i*sizeof(arrCnt));
  
  for (;i > 0; i--;)
    arrCheck[0] = gen::gfx();  
    arrCheck[0] = gen::inpt();
    arrCheck[0] = gen::snd(); };   
  al_init()